name: Playwright E2E and API Tests 2
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env:
      MOVIEDB_ACCESS: yI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.l69bunZBw-Ifr69_oG-6GBIb0F7FUFUJ-taXP2lcM3c
      MOVIEDB_APIKEY: d71a2265cfe64683adaeeb7ff4e6203e
      MOVIEDB_PASS: hibihabi
      MOVIEDB_USER: vlaco1984
    container:
      image: mcr.microsoft.com/playwright:v1.39.0-jammy
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 18
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright Tests
      run: xvfb-run npm run test
    - uses: actions/upload-artifact@v3
      if: always()
      with:
        name: playwright-report
        path: my-report/
        retention-days: 30
